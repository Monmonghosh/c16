#include<stdio.h>
int main() {
    float area,d, a;
    printf("enter the length of side:");
    scanf("%f", &d, &area, &a);
    area = 3 * 1.73 * a * a / 2;
    printf("area of hexagon=%f, area ");

}